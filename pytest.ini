[pytest]
addopts = -ra --browser chromium --alluredir=allure-results --clean-alluredir --junitxml=junit.xml
testpaths = tests
markers =
    api: API tests for HTTP Lab endpoints
    ui: browser tests for lesson 4 page
